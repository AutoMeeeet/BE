<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.teamproject.meeting.mapper.UsersMapper">
    <select id="existsByEmail" parameterType="string" resultType="boolean">
    	SELECT EXISTS(
    		SELECT 1
    		FROM USERS
    		WHERE email = #{email}
    	)
    </select>
    
    <insert id="saveUsers" parameterType="com.teamproject.meeting.entity.Users"
    	useGeneratedKeys="true" keyProperty="userId">
    	INSERT INTO USERS (email, password, nickname, provider, role)
    	VALUES (#{email}, #{password}, #{nickname}, #{provider}, #{role})
    </insert>
    
    <select id="findByEmail" parameterType="string" resultType="com.teamproject.meeting.entity.Users">
    	SELECT user_id, email, password, role, nickname
    	FROM USERS
    	WHERE email = #{email}
    </select>
    
    <update id="changeNickname">
    	UPDATE USERS
    	SET nickname = #{nickname}
    	WHERE user_id = #{userId}
    </update>
    
    <select id="findPasswordHashByUserId" resultType="string">
    	SELECT password
    	FROM USERS
    	WHERE user_id = #{userId}
    </select>
    
    <update id="changePW">
    	UPDATE USERS
    	SET password = #{encodedPW}
    	WHERE user_id = #{userId}
    </update>
</mapper>